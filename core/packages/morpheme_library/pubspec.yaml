name: morpheme_library
description: A new Flutter package project.
version: 0.0.1

publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter

  morpheme_base: ^1.2.0
  morpheme_http: ^1.4.0
  morpheme_extension: ^1.0.1
  morpheme_cached_network_image: ^1.1.3
  morpheme_responsive: ^0.0.4

dev_dependencies:
   dev_dependency_manager:
    path: ../dev_dependency_manager

flutter:
  uses-material-design: true
