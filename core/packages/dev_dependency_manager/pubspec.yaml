name: dev_dependency_manager
description: A new Flutter package project.
version: 0.0.1

publish_to: "none"

environment:
  sdk: "^3.6.0"
  flutter: "^3.27.0"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter

  # Navigation
  go_router: ^14.8.1

  # Test
  mocktail: ^1.0.4
  bloc_test: ^10.0.0

  # Linter
  flutter_lints: ^6.0.0

flutter:
